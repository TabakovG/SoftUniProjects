function subtract() {
    let target = document.getElementById('result');
    let first = Number(document.getElementById('firstNumber').value);
    let sec = Number(document.getElementById('secondNumber').value);
    target.textContent = first - sec;
    console.log('TODO:...');
}